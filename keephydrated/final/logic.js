//function declaration named litres with parameter time
function litres(time) {
/*return Math.floor() with three paramters time, multiplicatio sign,
and 0.5*/
	return Math.floor(time * 0.5);
}

//very simple function
