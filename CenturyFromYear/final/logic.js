/*
variable with identifier century = assignment operator, year parameter
with the following expression Math.ceil() function with parameter
year devided by 100
*/

const century = year => Math.ceil(year/100)


/*An arrow function expression is a compact alternative to traditional
function expression, but is limited and can't be used in all situations.
