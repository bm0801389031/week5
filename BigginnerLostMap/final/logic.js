//declare function named maps with parameter x
function map(x){
/*return map method with x property[map targets each element
in the array with the provided function, and creates a new array]
(function name n fatarrow to declare function n times 2);*/
	return x.map(n => n * 2);
}